# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Real credentials. .env.example is the tracked template and holds no secrets.
.env
.env.*
!.env.example

# Worker runtime variables for `wrangler dev`. The deployed values live in the
# Cloudflare dashboard; this file is the local mirror and is never committed.
.dev.vars

# Wrangler's local state and build cache.
.wrangler

# Claude Code local files
.claude/skills/
!.claude/settings.local.json

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
